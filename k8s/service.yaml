apiVersion: v1
kind: Service
metadata:
  name: fraud-detection-claims-api
spec:
  selector:
    app: fraud-detection-claims-api
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
